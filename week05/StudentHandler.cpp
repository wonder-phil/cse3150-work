#include <iostream>

#include "Student.h"

int main() {

    Student myStudent = load_student();

    cout << myStudent << endl;

    Student studentr;
        bool ok = readStudent(s);
        if (!ok) break; // Ctrl+D
        students.push_back(std::move(s));
        std::cout << "\n";

}